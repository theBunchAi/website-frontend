import { EventsHeroBanner, EventCards } from "@/modules/events";

export default function Events() {
  return (
    <section id="events" aria-label="Bunch Ai | Events">
      <EventsHeroBanner />
      <EventCards />
    </section>
  );
}
